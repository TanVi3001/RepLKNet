# Ảnh demo

Notebook tự tạo `demo_shapes.png` nếu thư mục này chưa có ảnh.

Để dự đoán ảnh thật, đặt một hoặc nhiều ảnh `.jpg`, `.jpeg`, `.png`, `.bmp` hoặc
`.webp` vào đây rồi chạy lại notebook. Notebook luôn chọn ảnh đầu tiên theo thứ tự
tên file.
